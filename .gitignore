# Секреты — никогда не коммитить и не класть в Docker-образ
.env

# База данных — это данные, а не код; в проде она должна жить в volume, не в образе
*.db
*.db-journal
*.sqlite3

# Python
__pycache__/
*.pyc
venv/
.venv/

# Логи
*.log
