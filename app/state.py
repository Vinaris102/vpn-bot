"""
Небольшой модуль для данных, общих между app/bot.py и app/admin.py,
которые иначе привели бы к циклическому импорту (bot.py импортирует
роутер из admin.py, поэтому admin.py не может импортировать bot.py напрямую).
"""

bot_username: str | None = None  # заполняется в app/bot.py::main() при старте
